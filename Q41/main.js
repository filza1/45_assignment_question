// Define the function for each magicain name from an array
function show_magician(magicain) {
    magicain.forEach(function (name) { return console.log(name); });
}
// Define an array containing magician name
var magicain_name = ["Harry potter", "Arshman", "Abdullah"];
// cal the function to print each magician name
show_magician(magicain_name);
